---
name: rd-production-summary
description: 查询研发中心项目投产情况汇报数据，包括已上线项目概览、上线清单列表、拟上线清单、待投产需求点（总量旁数据源分类 + 7 类产品线分布）、上个月两项能力分析（业务场景能力提升、基础服务能力夯实）、按产品线分节的重点功能分析。用户询问投产/上线/汇报/清单/待投产/产品线分布/窗口类型/能力分析/重点功能，或「上月已上线情况」「本月投产汇报」等时使用；通过脚本调用接口获取实时数据并作答。
---
# 研发中心项目投产情况汇报 Skill

## 目标
调用研发效能平台接口，获取指定月份的投产汇报实时数据，回答用户对投产情况的查询与汇总诉求；完整月度汇报在出数后基于清单归纳能力分析与重点功能分析。

## 使用边界
1. 量化数据均来自接口实时返回，不依赖静态手册正文。
2. 用户未指定月份时，默认使用当前自然月（格式 `YYYY-MM`）。
3. 环境由脚本内部自动决定，回复中不提及任何环境相关信息。
4. 接口返回非 `M0200` 时，明确告知用户调用失败及错误信息，不编造数据。
5. 完整汇报中的分析模块允许基于接口清单做归纳表述，但必须可追溯到清单字段，禁止无依据编造银行名、功能或数量。

## 目录约定
- 核心规则：`SKILL.md`
- 接口脚本：`scripts/`
- 问答提示词：`prompt/`
- 参考手册：`references/`
- 素材资源：`assets/`

---

## 意图识别总表（优先阅读）

先判定意图档位，再选接口与是否输出分析。多意图并存时按「完整汇报 > 专项分析 > 清单明细 > 单点数字」取最高档，可叠加拉数。

| 意图档位 | 典型问法（含近义） | 必调脚本 | 是否输出分析 |
|----------|-------------------|----------|--------------|
| A. 单点概览数字 | 「上线了多少项目」「功能项数多少」「常规/临时/紧急各多少」「产品线分布」「按日分布」「排期统计」 | `overview`（按已上线/计划选 `viewType`） | 否 |
| B. 已上线清单 | 「上线清单」「已上线列表」「哪些项目上线了」「上线明细」「按产品线看已上线项目」 | `launchList`；若还需总数可加 `overview(LAUNCHED)` | 否（除非同时命中 E/F/G） |
| C. 拟上线清单 | 「拟上线清单」「计划上线列表」「下月要上哪些」「即将上线项目」「排期中的项目明细」 | `keyFeatures`；可选 `overview(PLANNED)` | 否（除非同时命中 F/G） |
| D. 待投产需求 | 「待投产」「待上线需求」「还有多少没上线」「积压需求」「需求来源部门」 | `pendingDemands` | 否 |
| E. 仅上月汇总/能力 | 「上月已上线情况」「上个月投产汇总」「上月汇报」「上月能力分析」「业务场景能力提升」「基础服务能力夯实」（未同时要求本月） | `overview(LAUNCHED)` + `launchList`（目标月=上月） | **是**：业务场景能力提升 + 基础服务能力夯实 |
| F. 仅本月完整汇报 | 「本月投产情况」「本月完整汇报」「7 月投产汇总」「月度投产汇报」（未同时要求上月）、「重点功能分析」（未同时要求上月能力） | `overview(LAUNCHED)` + `launchList` + `keyFeatures`；按需 `pendingDemands` / `overview(PLANNED)` | **是**：重点功能分析（按产品线分节） |
| G. 上月+本月完整汇报 | 「上月和本月投产」「含上月能力分析的月度汇报」「完整月度汇报（含能力+重点功能）」「对比上月看本月」 | 本月：`overview`+`launchList`+`keyFeatures`；上月/`prevMonth`：`overview`+`launchList`；按需 `pendingDemands` | **是**：能力分析两项 + 重点功能分析（按产品线分节） |

### 月份词归一

| 用户说法 | 归一结果 |
|----------|----------|
| 未提月份 / 「本月」「这个月」「当月」 | 当前自然月 `YYYY-MM` |
| 「上月」「上个月」「上一月」 | 当前自然月的上一个月 |
| 「下月」「下个月」 | 当前自然月的下一个月（拟上线/计划类优先） |
| 「6 月」「2026-06」「2026年6月」 | 对应 `YYYY-MM`；年份缺失时用当前年 |
| 「上月和本月」「这两个月」 | 同时取上月 + 本月 → 意图 G |
| 「近两月」「月度汇报含上月」 | 同时取上月 + 本月 → 意图 G |

### 已上线 vs 计划上线（`viewType`）

| 线索 | viewType |
|------|----------|
| 已上线、实际上线、完成投产、上线了多少、上月/本月已上线情况 | `LAUNCHED` |
| 计划上线、拟上线、即将上线、下月排期、计划投产 | `PLANNED` |
| 仅说「投产情况/汇报」未区分 | 默认 `LAUNCHED`；完整汇报中计划侧用 `keyFeatures` / 可选 `overview(PLANNED)` |
| 同时问已上线与计划 | 两侧都拉，分段展示 |

### 完整汇报 vs 单点（分析开关）

**打开分析（先明细后分析）**——命中任一即视为汇总/汇报，不只回数字：
- 含「情况」「汇总」「汇报」「总结」「盘点」「整体看看」「全面」「完整」等汇总词，且指向投产/上线
- 明确要「能力分析」「业务场景能力提升」「基础服务能力夯实」「重点功能分析」
- 意图表中的 E / F / G

**不打开分析**——只出数或清单：
- 纯数量/分布：多少、数量、各多少、占比、分布图数据
- 纯清单：清单、列表、明细、有哪些（未夹带情况/汇总/分析）
- 纯待投产：待投产/积压（未要求完整汇报）

### 专项分析句（可单独触发对应模块）

| 用户明确提到 | 行为 |
|--------------|------|
| 业务场景能力提升 / 基础服务能力夯实 / 上月能力分析 / 上个月两项能力 | 拉上月 `launchList`（+可选 overview），输出对应两项（或用户点名的一项，建议两项都出以保持版式完整） |
| 重点功能分析 / 业务重点功能 / 基础服务重点功能 | 拉本月（或指定月）`keyFeatures`，输出重点功能分析（按产品线名分节，无业务/基础服务中间层） |
| 只要「云信上月上了啥」等单产品线清单 | 拉 `launchList` 后过滤展示；**不**升格为完整能力分析，除非同时要分析/汇总 |

### 消歧与边界

1. 「上月已上线清单」→ 意图 B（清单）；「上月已上线**情况/汇总**」→ 意图 E（含能力分析）。
2. 「本月上线了多少」→ 意图 A；「本月投产**情况/汇报**」→ 意图 F。
3. 只说「能力分析」未说月份 → 按上月做「上个月两项能力分析」（与模块语义一致）。
4. 只说「重点功能分析」未说月份 → 按本月 `keyFeatures`。
5. 「月度投产汇报」未提上月 → 意图 F；若补「含上月/能力分析/对比上月」→ 意图 G。
6. 指定历史月（如「5 月已上线情况」）→ 将该月视为「查询月」：要情况/汇总时，用该月 `launchList` 做能力分析版式（业务场景能力提升、基础服务能力夯实），不强制再拉其上一月，除非用户还要「再对比前一个月」。
7. 接口失败或空数据：如实说明，不编造分析要点。

---

## 环境配置

| 环境 | base URL |
|------|----------|
| test | `http://103.234.22.57:8090/zqyl-pm-api` |
| prod | `http://172.16.18.30:58184/zqyl-pm-api` |

切换方式：所有脚本均支持 `--env test` 或 `--env prod`（默认 `prod`）。

---

## 产品线分类标准（全局统一）

展示与分析时统一归入以下 **7 类**（固定顺序；无数据可省略该行/节，顺序不变）：

1. 链数  
2. 云链证  
3. 云信  
4. 链信APP  
5. 信企直连  
6. 基础服务  
7. 其他（其余一律归入）

**匹配规则**：
- 对接口 `productLine` 去掉空格后归一：`链信 APP` / `链信APP` / `链信客户端` → **链信APP**；名称包含或等于前 6 类标准名则归入对应类；否则 → **其他**。
- 适用范围：待投产整体概览、待投产/拟上线/已上线项目清单分组、重点功能分析分节。
- 待投产脚本已输出 `canonicalGroups`（按上述 7 类重映射），展示优先用该字段。

---

## 接口一：投产概览

**用途**：查询指定月份的项目数、功能项数、排期项目数（按上线窗口类型分布）。

- 接口地址：`{BASE_URL}/report/productionSummary/overview`
- 请求方式：`GET`
- 参数：
  - `yearMonth`（必填）：月份，格式 `YYYY-MM`
  - `viewType`（必填）：`LAUNCHED`（已上线）或 `PLANNED`（计划上线）
- 返回字段含义：
  - `viewType`：视图类型
  - `titleSuffix`：标题后缀
  - `projectCount`：项目数
  - `featureCount`：功能项数
  - `topLineTypeStats`：上线窗口类型统计列表（含 `typeName`、`count`）
  - `dailyDistribution`：按日期分布
  - `productLineDistribution`：按产品线分布
- 触发规则（命中任一即调用；完整汇报 E/F/G 亦必调）：
  - 数量类：「上线了多少项目」「项目数」「功能项数」「功能点数」「投产了几个」
  - 概览类：「投产概览」「投产情况」（若仅为数字诉求）「汇报数据」「整体数据」「看一下规模」
  - 窗口类：「常规/临时/紧急各多少」「上线窗口类型」「排期类型统计」「排期项目数」
  - 分布类：「产品线分布」「哪些产品线上得多」「按日分布」「哪天上线多」
  - `LAUNCHED` = 已上线；`PLANNED` = 计划/拟上线；未区分时默认 `LAUNCHED`

## 接口二：上线清单

**用途**：获取已上线项目的详细列表（按产品线分组，分页）。

- 接口地址：`{BASE_URL}/report/productionSummary/launchList`
- 请求方式：`GET`
- 参数：
  - `yearMonth`（必填）：月份，格式 `YYYY-MM`
  - `viewType`：固定为 `LAUNCHED`
  - `page`（可选，默认 `1`）
  - `pageRow`（可选，默认 `20`）
- 返回字段含义（脚本规范化后）：
  - `grandTotal`：已上线项目总数
  - `groups`：按产品线分组，每组含 `productLine`、`total`、`items`
  - `list`：平铺列表（与 groups 内 items 等价，便于遍历）
  - `items` / `list` 每项含：
    - `projectId`：项目 ID
    - `jiraId`：项目编号（接口字段；兼容旧名 `jiraNo`）
    - `projectName`：项目名称
    - `productLine`：产品线（来自分组或 `productLineName`）
    - `keyValue`：键值
    - `description`：项目描述（接口字段 `projectDescribe`）
    - `windowType`：上线窗口类型名称（接口字段 `topLineTypeName`）
    - `topLineType`：上线窗口类型码
    - `onlineDate`：上线日期（接口多为 `MM.DD` 字符串，如 `07.10`）
- 触发规则（命中任一即调用；意图 E/F/G 及能力分析亦必调，需分页拉全）：
  - 「上线清单」「已上线项目列表」「已上线明细」「哪些项目上线了」「上了哪些需求/项目」
  - 「按产品线看已上线」「某产品线本月/上月上线项目」「导出上线列表」
  - 「业务场景能力提升」「基础服务能力夯实」「上月能力分析」（作为分析数据源）
  - 用户点名 Jira 号/项目名核验是否已上线时，拉清单后匹配作答

## 接口三：拟上线清单

**用途**：获取计划上线月份的拟上线项目清单（按产品线分组，分页）。

- 接口地址：`{BASE_URL}/report/productionSummary/keyFeatures`
- 请求方式：`GET`
- 参数：
  - `yearMonth`（必填）：月份，格式 `YYYY-MM`
  - `viewType`：固定为 `PLANNED`
  - `page`（可选，默认 `1`）
  - `pageRow`（可选，默认 `20`）
- 返回字段含义：
  - 数据按产品线分组，每组含 `productLine`、`total`、`items`
  - `items` 中每项含：`projectId`、`jiraId`、`projectName`、`keyValue`、`description`、`topLineType`、`onlineDate`
- 触发规则（命中任一即调用；意图 F/G 及重点功能分析亦必调）：
  - 「拟上线清单」「计划上线项目列表」「下月上线清单」「即将上线」「下一步排期清单」
  - 「本月/指定月计划投产有哪些」「按产品线看拟上线」
  - 「重点功能分析」「业务重点功能」「基础服务重点功能」（作为分析数据源）

## 接口四：待投产需求点

**用途**：获取尚未投产的需求点列表，按产品线分组；脚本额外输出 7 类归并与数据源分类汇总。

- 接口地址：`{BASE_URL}/report/productionSummary/pendingDemands`
- 请求方式：`GET`
- 参数：
  - `yearMonth`（必填）：月份，格式 `YYYY-MM`
- 返回字段含义（脚本规范化后）：
  - `grandTotal`：待投产需求总量（与「合计」同源）
  - `groups`：接口原始产品线分组，每组含 `productLine`、`total`、`items`
  - `canonicalGroups`：按「产品线分类标准」归并后的分组（优先用于展示）
  - `dataSourceSummary`：按 `dataSource` 聚合，含 `code` / `label` / `count`
  - `items` 每项含：`code`、`name`、`productLine`、`description`、`planOnlineDate`、`reqDepartment`、`dataSource`
  - `dataSource` 枚举与展示名：
    - `DEMAND` → 待投产的需求点
    - `NEED_DESIGNING` → 产品源设计中
    - `NEED_DESIGNED` → 产品源设计完成
    - `PROJECT` → 项目数据
- 触发规则（命中任一即调用；完整汇报中用户顺带问「还有哪些没上」时按需调用）：
  - 「待投产需求」「待上线需求点」「还有多少需求没上线」「未投产」「积压需求」
  - 「待投产按产品线」「需求来源部门」「谁提的还没上」
  - 「本月汇报顺带看下待投产」→ 在 F/G 流程末尾追加
  - 「整体概览 / 待投产需求总量」→ 按下方「待投产整体概览」版式输出

### 待投产整体概览版式（图一 + 图二联动）

在「待投产需求总量」旁展示数据源分类条，下方跟产品线分布表；三者共用同一 `grandTotal`：

```text
待投产需求总量：{grandTotal} 条（分布在 {canonicalGroups 有数据的类别数} 条产品线）
合计：{grandTotal}｜待投产的需求点 {DEMAND}｜产品源设计中 {NEED_DESIGNING}｜产品源设计完成 {NEED_DESIGNED}｜项目数据 {PROJECT}
```

| 产品线 | 待投产数量 | 占比 |
|--------|------------|------|
| 链数 / 云链证 / … / 其他（按固定顺序，有数据才列） | `canonicalGroups[].total` | total / grandTotal |
| 合计 | grandTotal | 100% |

注：跨产品线需求按归属产品线统计；分类条四类之和须等于 `grandTotal`。

---

## 接口调用方式

提供 PowerShell 和 Python 两套脚本，功能完全等价。

### 环境自动切换（PowerShell 推荐用法）

```powershell
# 会话开始时设置一次，之后所有脚本自动使用测试环境
$env:RD_ENV = "test"

# 切回生产环境
$env:RD_ENV = "prod"
```

### PowerShell 调用示例

- 查询已上线概览（自动读取 `$env:RD_ENV`）：
  `powershell -ExecutionPolicy Bypass -File "scripts/get-production-overview.ps1" -YearMonth 2026-07`

- 单次指定测试环境：
  `powershell -ExecutionPolicy Bypass -File "scripts/get-production-overview.ps1" -Env test -YearMonth 2026-07 -ViewType PLANNED`

- 查询上线清单（第 1 页，每页 20 条）：
  `powershell -ExecutionPolicy Bypass -File "scripts/get-launch-list.ps1" -YearMonth 2026-07 -Page 1 -PageRow 20`

- 查询拟上线清单：
  `powershell -ExecutionPolicy Bypass -File "scripts/get-key-features.ps1" -YearMonth 2026-07`

- 查询待投产需求：
  `powershell -ExecutionPolicy Bypass -File "scripts/get-pending-demands.ps1" -YearMonth 2026-07`

### Python 调用示例（需安装 Python 3）

- 查询已上线概览（生产）：
  `python scripts/get-production-overview.py --yearMonth 2026-07 --viewType LAUNCHED`

- 查询计划上线概览（测试环境）：
  `python scripts/get-production-overview.py --env test --yearMonth 2026-07 --viewType PLANNED`

- 查询上线清单：
  `python scripts/get-launch-list.py --yearMonth 2026-07 --page 1 --pageRow 20`

- 查询拟上线清单：
  `python scripts/get-key-features.py --env test --yearMonth 2026-07`

- 查询待投产需求：
  `python scripts/get-pending-demands.py --yearMonth 2026-07`

---

## 完整汇报分析模块

仅在意图 **E / F / G**（或专项分析句）时，在量化数据之后强制输出分析；意图 A–D 默认不展开。  
**先列明细，再列分析。** 范围判定见上方「意图识别总表」，摘要如下：

| 查询范围 | 对应意图 | 输出分析模块 |
|----------|----------|--------------|
| 仅查询上月（含「上月已上线情况」） | E | 「上个月两项能力分析」：业务场景能力提升、基础服务能力夯实 |
| 仅查询本月 | F | 仅「重点功能分析」 |
| 同时涉及上月以及本月 | G | 「上个月两项能力分析」+「重点功能分析」 |
| 指定历史月「X 月已上线情况/汇总」 | 类 E | 以该月 `launchList` 输出业务场景能力提升、基础服务能力夯实 |
| 仅点名「能力分析 / 业务场景 / 基础服务夯实」 | 专项→E | 同上月两项（缺省月份=上月） |
| 仅点名「重点功能分析」 | 专项→F | 重点功能分析（缺省月份=本月） |

| 模块 | 数据来源 | 输出结构 | 何时输出 |
|------|----------|----------|----------|
| 上个月两项能力分析 | 上月或指定历史月已上线清单（`get-launch-list`，可选 overview）；意图 G 时为 `prevMonth` | 业务场景能力提升、基础服务能力夯实（小标题用产品线名，不加「产品线」后缀） | 意图 E、G，或专项能力分析句；指定月「已上线情况/汇总」 |
| 重点功能分析 | 查询月拟上线清单（`get-key-features`） | 在「重点功能分析」下按 7 类产品线名直接分节（无业务/基础服务中间层；标题如「链数：」） | 意图 F、G，或专项重点功能句 |

归类与文风见 `prompt/production-summary-qa.prompt.md`；版式见 `references/capability-analysis-template.md`。

完整汇报建议拉数顺序：
1. （意图 E / 指定历史月情况）`overview(LAUNCHED)` / `launchList` 目标月 → 明细后做能力分析两项
2. （意图 F / G）`overview(LAUNCHED)` 查询月
3. （意图 F / G）`launchList` 查询月
4. （意图 G）`overview` / `launchList` 上个月（`prevMonth`）
5. （意图 F / G）`get-key-features` 查询月
6. （意图 D 或 F/G 顺带问待投产）`pendingDemands`

## 执行流程

1. 用「意图识别总表」判定档位 A–G（及专项分析句）；多意图取最高档并可叠加。
2. 确认月份（见「月份词归一」）；意图 E/G 或「上月」类问法时计算上月 / `prevMonth`。
3. 确认环境（用户未提及时默认 `prod`）；回复中不提环境。
4. 按档位调用脚本，分页拉全清单类数据，解析 JSON stdout。
5. 结构化展示关键字段；时间戳额外展示 `YYYY-MM-DD`。
6. 若需分析：先明细后分析——E → 能力分析两项；F → 重点功能分析；G → 能力分析 + 重点功能分析。
7. 接口失败或空数据时明确说明，不编造分析。

## 回答格式

单点查询（意图 A–D）默认结构：
1. 结论（1-2 句汇总）
2. 关键数据（表格或列表）
3. 注意事项（数据时效；不提及环境）

意图 D（待投产）优先按「待投产整体概览版式」输出：总量旁附 `dataSourceSummary` 分类条 + `canonicalGroups` 产品线表；明细清单亦按 7 类顺序分组。

已上线 / 拟上线清单展示时，按「产品线分类标准」7 类顺序分组（无法归入前 6 类的归「其他」）。

完整汇报 / 专项分析（意图 E–G）输出顺序（先明细，后分析）：
1. 查询月投产概览与分布
2. 上线 / 拟上线 / 待投产等明细（按需；待投产用整体概览版式）
3. （意图 E、G 或专项能力分析）上个月两项能力分析
   - 业务场景能力提升
   - 基础服务能力夯实
4. （意图 F、G 或专项重点功能）重点功能分析  
   - 直接以产品线名为标题分节（链数、云链证、云信、链信APP、信企直连、基础服务、其他；不加「产品线」后缀；不输出「业务重点功能分析 / 基础服务重点功能分析」中间层）

## 缺失信息处理

接口返回为空或字段缺失时，按以下格式反馈：
- 当前结论：接口返回暂无数据。
- 建议操作：确认月份是否正确，或切换环境后重试。